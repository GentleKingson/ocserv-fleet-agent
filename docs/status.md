# Project Status

This page reflects the current source tree. It is not a production-complete
claim, and planned items are not available unless marked implemented.

## Security And Contract Baseline

| Contract | Current source-of-truth state |
| --- | --- |
| Controller schema | SQLite schema version `27`, migrations `1` through `27`: core registry/audit, enrollment, endpoint trust, observability, strict constraints, retention indexes, health policy, alert webhooks, closed versioned payloads, fenced scheduler job claims, scheduler maintenance, durable health evaluation runs, fenced alert delivery, append-only health history, deterministic health rollups, unbiased five-minute slot semantics, bounded advisory node metadata, and latest closed capability observations. |
| Generic allowed RPC methods | `node.ping`, `node.info`, `node.capabilities`, `probe.controller.ping`, `ocserv.service.summary`, `ocserv.version`, `ocserv.sessions.summary`, `ocserv.cert.expiry`, `ocserv.config.fingerprint`. Authorization also admits controller-only `probe.path.echo` and peer-only `probe.peer.echo`; neither is a generic caller capability. |
| Known but not allowed | `ocserv.service.status`, `ocserv.status`, `ocserv.reload`, `ocserv.restart`, `ocserv.config.apply`, `ocserv.config.rollback`, `ocserv.session.disconnect`, `ocserv.users.list`, `ocserv.users.get`, `ocserv.logs.recent`, `ocserv.cert.status`, `ocserv.config.summary`. |
| API | Twenty-three declared `GET` paths only. Non-loopback startup requires a private bearer token file and a TLS-terminating deployment boundary; the API itself does not terminate TLS. |
| Alert delivery | Scheduler evaluation only upserts local alert events. JSONL/webhook delivery is an explicit CLI action and never runs from the API/dashboard. |
| Controlled writes | Feature-gated DTO/config validation only. Default build and feature build contain no live dispatch; session disconnect has no safe selector and is rejected by the scaffold. |

| Feature | Current status | Main CLI | Main limits |
| --- | --- | --- | --- |
| Node registry | implemented | `ocfleet node add`, `ocfleet node list`, `ocfleet node enable`, `ocfleet node disable`, `ocfleet node remove`, `ocfleet node info` | Controller-local SQLite registry only; no discovery or automatic trust. Add/enable/disable/remove bind the resolved actor and commit registry/trust changes with their audit row in one transaction. Enable requires a clean bidirectional Active binding; removal revokes the unique Active trust before deleting the node. |
| Enrollment | implemented hardened slice | `ocfleet enroll token create`, `ocfleet enroll token revoke`, `ocfleet enroll request create`, `ocfleet enroll request reject`, `ocfleet enroll approve`, `ocfleet enroll claim` | Token/request transitions use actor-bearing atomic writers, stable optional request IDs, compare-and-set usage, closed terminal states, and low-sensitive audit. Schema 16 stores token labels/scope and request labels in closed kind-bound scalar maps; readers expose only validated public objects. Approval atomically creates an operator-owned registry node and bound Active generation-1 trust. `claim` strictly repairs legacy approved-unbound rows. No identity is derived from hostname/labels and no automatic reconciliation exists. |
| Endpoint lifecycle | implemented | `ocfleet endpoint rotate`, `ocfleet endpoint revoke`, `ocfleet endpoint quarantine` | Closed controller-local lifecycle: Active may rotate/revoke/quarantine; Quarantined may rotate/revoke; Revoked is terminal; Rotated accepts only an exact linked retry. Exact no-ops do not bump generation or audit. Rotation moves the node pointer atomically, while revoke/quarantine disable the current node. No diagnostic shell or service control. |
| Trust diff | implemented | `ocfleet trust diff`, `ocfleet trust diff --endpoint <endpoint-id>`, `ocfleet trust diff --format json`, `ocfleet trust diff --strict` | Reports controller trust drift; does not auto-remediate or create trust. |
| Trust policy GitOps | operationally mature B6 | `ocfleet trust policy validate|diff|sign|plan|approve`, `ocfleet trust policy history record|list` | Canonical signed revisions, separately pinned policy and reviewer Ed25519 keys, deterministic bounded CI plans, private Markdown, closed drift projection, trusted actor/key-bound approval, and locked bounded append-only history form a review-only chain. Truncated plans cannot be approved. Immutable SQLite review rejects migration and active WAL; missing state remains in memory. No apply command, agent contact, enrollment approval, or trust/path mutation exists. |
| Fixed RPC | implemented | `ocfleet ping`, `ocfleet node info`, `ocfleet probe ping`, `ocfleet probe path`, `ocfleet probe summary`, `ocfleet probe topology`, `ocfleet probe history`, `ocfleet probe observe` | Fixed methods and typed responses only; no raw command, file, log, or relay interface. Authorization requires an enabled registry node, matching node/EndpointID pointers, and exactly one Active trust binding. Missing, inactive, unbound, mismatched, or ambiguous state is rejected before key loading or network I/O, with complete scheduler source/target rechecks after concurrency waits. |
| ocserv readonly | implemented | `ocfleet ocserv status`, `ocfleet ocserv cert`, `ocfleet ocserv sessions summary` | Low-sensitive fixed RPCs only; controller cannot pass paths, commands, service units, or selectors. |
| Path probe | implemented | `ocfleet probe path <source-node-id> <target-node-id>`; `ocfleet schedule job add --kind path-probe --source-node-id <source> --target-node-id <target> --interval <duration>` | Explicit source/target pair only; no mesh enumeration, generic payloads, or multi-hop probing. |
| Scheduler | operationally mature A3 | `ocfleet schedule job add --name <name> --kind <kind> --interval <duration> [--jitter-seconds <n>] [--timeout-ms <n>] [--selector role=<role>|node_id=<node-id>]`, `ocfleet schedule job list --json`, `ocfleet schedule job show <job-id> --json`, `ocfleet schedule job validate <job-id> --json`, `ocfleet schedule job enable`, `ocfleet schedule job disable`, `ocfleet schedule run --once [--job-id <job-id>] [--json]`, `ocfleet schedule run list --limit <n> --json`, `ocfleet schedule run show <run-id> --json`, `ocfleet schedule maintenance set|show|clear`, `ocfleet schedule daemon`, `ocfleet schedule status --json` | CLI and SQLite job/run/observation path exists; fixed job kinds only. Schemas 19-20 add audited deterministic claims, monotonic fences, active-run binding, expiry takeover, abandoned-run recovery, renewable leases, bounded misfire coalescing, transient-only retry/backoff under a hard per-tick budget, audited global maintenance suppression, graceful drain/restart, bounded per-attempt timeouts, and deterministic bounded jitter. Selector and explicit-pair storage remains closed and cannot infer trust. The A3 completion inventory records the acceptance evidence. Web/API is read-only and cannot trigger scheduler work. |
| Observations | partially implemented / active implementation | `ocfleet observation list --node <node-id> --method <method> --limit <n> --json`, `ocfleet observation show <observation-id> --json` | Bounded stored observation queries only. Schema 11 stores closed method/result-class-bound v1 summaries; migration canonicalizes exact legacy objects and rejects contamination. CLI/API readers expose fixed public fields, never the storage envelope or raw RPC response bodies. |
| Health | operationally mature A4 | `ocfleet health summary --json`, `ocfleet health node <node-id> --json`, `ocfleet health evaluator run --json`, `ocfleet health evaluator daemon --interval-seconds <n>`, `ocfleet health snapshot list --limit <n> --json`, `ocfleet health policy show`, `ocfleet health policy set` | Derived advisory state from controller SQLite observations. Schema 21 persists watermark-, policy-, and computation-version-bound evaluation runs; actor-bound atomic writers commit snapshots, typed failures, recovery, and audits. The independent evaluator updates snapshots without dashboard reads, drains/restarts on signals, and never runs agent RPC or mutates trust, nodes, or scheduler state. Closed health payload readers still fail on contamination without exposing raw input. |
| Health history, rollups, and SLOs | operationally mature B1 | `ocfleet health history ...`, `ocfleet health rollup recompute|list|refresh ...`, `ocfleet health slo --window <24h|7d|30d> --to <rfc3339> ...`, `GET /health/slo` | Schemas 23-25 provide append-only history, deterministic 5m/1h/1d rollups, and one latest status per five-minute slot. Fixed-window CLI/API projections expose explicit missing coverage, distinct status duration, availability, errors, per-bucket latency ranges, certificate risk, and drift. Deterministic closed-bucket refresh has a hardened network-isolated timer; the B1 inventory records completion evidence. |
| Node metadata and maintenance | operationally mature B2 | `ocfleet node metadata set|show`, `ocfleet node maintenance set|show|clear`, bounded scheduler selectors | Schema 26 stores bounded advisory environment, site, owner/team, tier, labels, expected version, and per-node maintenance independently from trust. Atomic audit, a 50-node selector cap, advisory policy diff, and read-only API projection are enforced. |
| Versioned read-only API | operationally mature B3 | `/api/v1/fleet/summary`, `/api/v1/nodes`, `/api/v1/health/history`, `/api/v1/alerts` | Signed filter-bound keyset cursors, half-open time windows, metadata/status/reason filters, ETag/304, correlated stable errors, strict queries, exact OpenAPI drift checks, and authenticated nginx Docker E2E pass without RPC or mutation routes. |
| Local snapshot producer SDK | operationally mature B4 | `ocfleet-snapshot-validator`, `ocfleet-snapshot-schema`, `minimal-producer` | Closed v2 aggregate schema, embedded JSON Schema, bounded validator, private atomic SDK output, agent provider compatibility, and non-root Docker permission/symlink/hardlink tests pass. No controller or RPC launch path exists. |
| HMAC configuration fingerprint | operationally mature B5 | `ocserv.config.fingerprint`, `ocfleet ocserv status` | Agent-local HMAC-SHA-256 uses bounded owner-only key files and stable key IDs. Current/previous digests support rotation without false drift, controller requests remain selector-free, CLI/storage expose only IDs and short prefixes, explicit legacy SHA-256 preserves its old wire shape, and Linux permission/link tests pass. |
| Protocol capability negotiation | operationally mature B7 | `ocfleet node capabilities <node-id> [--json]`, `node.capabilities` | Closed bounded protocol/method/provider/feature DTOs expose no paths, commands, units, policy, secrets, or raw config. Controller decode fails closed on invalid or incompatible data; legacy `METHOD_NOT_FOUND` is deterministic and never infers or enables a feature. Controlled-write compile/local booleans remain observational only. |
| Agent version governance | operationally mature B8 | `ocfleet version distribution|readiness`, `GET /api/v1/version/readiness` | Schema 27 atomically stores the latest closed capability observation with its RPC audit. Bounded SemVer policy/distribution, pre-release ordering, protocol/provider compatibility, readiness, and derived read-only alerts are projected to CLI/API/dashboard with unknown data kept unknown and all actions disabled. No install, package-manager, restart, or automatic upgrade path exists. |
| Read-only product gate | operationally mature B-READY | Stage B source tree; target `v0.4.0` | B1-B8 inventories, default/all-feature regression, non-root network-isolated Docker checks, bounded query/history contracts, and mixed-version tests pass. The default build remains read-only and Stage B adds no trust expansion, automatic upgrade, or agent control path. This is release readiness, not a published `v0.4.0` claim. |
| Alerts | operationally mature A5 | `ocfleet alert list`, `ocfleet alert hook add-webhook|list|test|enable|disable`, `ocfleet alert deliver`, `ocfleet alert worker run|daemon|status`, `ocfleet alert delivery-daemon`, `ocfleet alert silence`, `ocfleet alert resolve` | Schema 22 and actor-bound writers provide fenced idempotent webhook queueing, bounded recovery/retry/DLQ, attempt history, delivery health, and atomic audit. The automatic worker revalidates HTTPS public endpoints, allowlists, HMAC key IDs, body bounds, and no-redirect transport; applies global, per-group, and repeat limits; and drains/restarts on signals. JSONL remains an explicit manual path. No shell, script, command, template, or remediation hook exists. The A5 inventory records requirement evidence. |
| Retention | implemented hardened slice | `ocfleet retention show`, `ocfleet retention set <scope>`, `ocfleet retention explain --scope <scope> --json`, `ocfleet retention apply [--operation-id retention-<uuid>] --json` | Prunes only six fixed observation/history scopes, including health history and rollups independently from latest snapshots. `explain` and `apply --dry-run` neither delete nor audit. Policy changes and each non-dry-run scope apply are actor-bound atomic writers; stable operation IDs provide exact replay across multi-scope partial progress. Protected tables are never targets. |
| Audit export | partially implemented / active implementation | `ocfleet audit export --from <rfc3339> --to <rfc3339> --format jsonl --output <path> [--redact none/default/strict] [--include-checksum] [--sign-with-key-file <path>] [--max-rows <n>]` | Bounded controller audit window only; writes private JSONL output and optional checksum/Ed25519 signature sidecars. Schema 18 stores closed typed detail bound to the complete relational record; export fails closed on contamination and never exposes the storage envelope. |
| Backup and restore | operationally mature A6 | `ocfleet backup create|list|verify|inspect`, `ocfleet restore plan|apply` | Managed SQLite online backups use private files, identity-bound closed manifests, SHA-256, optional Ed25519 signatures, and integrity/schema verification. Restore is plan-first, requires `--yes`, pre-backs up existing state, handles WAL/SHM, atomically replaces the database, and rolls back injected post-replacement failure. SecretKey material is never included. The A6 inventory records requirement evidence. |
| Metrics | operationally mature A7 | `GET /metrics` on `ocfleet-api`; agent `--metrics-listen` | Controller and agent Prometheus exposition is loopback by default, bearer-protected on non-loopback listeners, and uses only fixed bounded labels. Agent transport, RPC result/duration, nonce, and audit durability metrics are live-instrumented; controller scheduler, RPC, observation, health, alert, delivery, SQLite, retention, and audit-export requirements are complete. The A7 inventory records acceptance evidence. |
| Release engineering | operationally mature A8 | `v0.3.0`; `Release Draft` workflow | Default/all-feature gates, fuzzing, migration/failure corpus, Chromium E2E, distro/architecture smoke, SBOMs, keyless signatures, GitHub provenance, support policy, and rollback runbook are complete. The published release has 38 independently verified assets; the A8 inventory records exact evidence. |
| Governance foundation | implemented hardened A1 slice | `ocfleet --actor <actor> ...`, `OCFLEET_ACTOR=<actor> ocfleet ...` | Unified actor resolver and fixed viewer/operator/security-admin policy vocabulary are implemented; local CLI does not enforce RBAC. API principals are viewer-only. Every production controller mutation family in the A1 inventory uses actor-bearing atomic `StoreWriter` boundaries; the source guard rejects SQL and direct-mutator bypasses. Fixture-only raw helpers are unreachable from production dispatch. |
| Web/API | partially implemented / active implementation | `ocfleet-api --database controller.sqlite --read-only --cursor-key-file ./cursor-keys.json --listen 127.0.0.1:8080` | Experimental read-only `GET` observation surface only with `docs/api/openapi.yaml`; uses the API-specific `ApiReadStore` over SQLite read-only connections. Every listener requires a private persistent cursor keyring; non-loopback listeners also require `--auth-token-file`. No API route triggers agent RPC, scheduler runs, alert mutations, retention changes, trust changes, or node registry changes. |
| Dashboard | partially implemented / active implementation | `GET /` from `ocfleet-api` | Static read-only dashboard for health, nodes, jobs, runs, observations, alerts, and a bounded audit export preview; sends CSP/referrer/content-type hardening headers; no non-GET fetch or control buttons. |
| Local ocserv collector | safety-constrained normalizer; no live discovery | `ocfleet-ocserv-collector --config <collector.toml> --output <path>`, `ocfleet-ocserv-collector --check --config <collector.toml>`, `ocfleet-ocserv-collector --print-example-config` | Local operator-managed reduced snapshot transform only; no service-manager/log/admin-tool passthrough, raw source capture, or controller-supplied path. It preserves producer `collected_at`, so timer runs cannot make static metadata fresh. This is not a completed live ocserv collector. |
| Controlled operations | planned / not implemented | Design only in `docs/controlled-write-operations.md`; draft DTOs are feature-gated and not wired to dispatch. | Default build remains read-only; no reload, restart, config apply/rollback, session disconnect, raw command, shell, script, raw file, or dashboard/API mutation route is implemented. |
